function dy = df2(x, y)
% 注意，x 是自变量，y 是因变量，由 y1、y2、y3 组成
dy = zeros(3, 1); % 初始化用来储存因变量一阶导数的列向量（不能写成行向量）
dy(1) = y(2) * y(3);
dy(2) = -y(1) * y(3);
dy(3) = -0.51 * y(1) * y(2);
% 上面四行可以写成一行：dy = [y(2) * y(3); -y(1) * y(3); -0.51 * y(1) * y(2)]
end